prefix: '&6&lPluginName >&r' # TODO: Đổi tên plugin

database:
  # Loại cơ sở dữ liệu
  # Các engine hỗ trợ: h2, mysql, mariadb
  # mặc định là h2 (lưu vào 1 file)
  engine: h2
  h2:
    file: pluginname # TODO: Đổi tên plugin
  mysql:
    host: localhost
    port: 3306
    user: 'root'
    password: '123'
    database: pluginname # TODO: Đổi tên plugin

# Bật/tắt tính năng kiểm tra cập nhật phiên bản mới
check-update: true
